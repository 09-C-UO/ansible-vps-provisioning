---
# Everything after bootstrap. Safe to re-run at any time (connects as admin).
- name: Apply the VPS security baseline
  ansible.builtin.import_playbook: hardening.yml
- name: Deploy Traefik
  ansible.builtin.import_playbook: traefik.yml
- name: Host the applications
  ansible.builtin.import_playbook: apps.yml
