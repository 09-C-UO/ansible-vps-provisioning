[defaults]
inventory = inventories/test/hosts.ini
host_key_checking = True
remote_user = admin
interpreter_python = auto_silent

[privilege_escalation]
become = true
become_method = sudo
become_user = root
# The sudo password comes from the vault (ansible_become_password).
become_ask_pass = false
