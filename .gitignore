# Secrets (encrypted or not, they never leave this machine)
*.vault.yml

# Local SSH material (keys belong in ~/.ssh, never in this repository)
*.pem
id_*

# Local lab state (keys, vault, known_hosts)
tests/lab/.work/

# Python bytecode
__pycache__/
